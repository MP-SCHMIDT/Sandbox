#pragma once


class Random
{
  public:
  static int Val(int const iMin, int const iMax);
  static float Val(float const iMin, float const iMax);
  static double Val(double const iMin, double const iMax);
};
